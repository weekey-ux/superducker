# SuperDucker（超级潜水员）—— Windows 下的轻量快速启动工具

> 项目地址：https://github.com/weekey-ux/superducker
> 下载地址见文末附件。

## 它解决什么问题？

在没有 Lucy、FastRun、Maye、Rolan、CLaunch、MaxLauncher 这类启动器之前，我们如何快速打开一个软件，又不用忍受桌面和开始菜单被塞得乱七八糟？

答案是：**`Path 环境变量` + 任意文件夹 + `Win+R`**。

SuperDucker 正是基于这个最简单、最不被打扰的思路构建的——它不劫持你的系统，只是让「运行一个程序」这件事变得更快、更有序。

---

## 三个组件，各司其职又能协同

SuperDucker 由三个独立可执行文件组成，它们**既能单独使用，也能互相配合**，构成一套完整的本地 + 局域网软件管理体系：

| 组件 | 形态 | 职责 |
|------|------|------|
| `superducker.exe` | GUI 图形界面 | 主程序：管理快捷入口、图标网格、设置，以及内置「本地软件商城」 |
| `sd.exe` | 命令行工具 | 在 `Win+R` 运行框里快速启动/直达目录/提权运行，并负责打包等运维操作 |
| `superducker-repo.exe` | 局域网商店服务 | 一台机器跑起来，就成了公司内网的「简易应用商店」服务器 |

三者通过同一套 **`localshop/` 绿色软件仓库目录** 与统一的打包格式（`.sdzip`）打通：本地打包 → 放进 `localshop/` → GUI 商城识别 → 客户端安装/升级；而 `sd.exe` 则把「运行」延伸到系统运行框里。

---

## 一、superducker.exe（GUI）使用简介

**1. 启动与主界面**

运行 `superducker.exe`，任务栏右下角会出现托盘图标，**双击**即可唤出主界面。

点击右上角**齿轮**进入设置页——里面都是快速启动类软件的常规参数（热键、位置、网格、外观等），一目了然。

**2. 内置本地软件商城**

点击右上角**「店」**图标进入商城。商城默认分三栏：**可安装 / 已安装 / 已卸载**。

- 干净的机器上商城是空的；
- 只要 `localshop/` 目录里放有打包好的程序，就会出现在「可安装」中，点一下即可装进 SuperDucker。

**3. 如何把绿色软件打包进商城？**

还记得设置页里的「打开打包页面」快捷键吗（示例为 `Alt+D`）？按下它唤起打包工具，按提示填入名称、路径、缩写、图标等，就能把一个绿色软件打成专用压缩包（`.sdzip`）导入程序。

切回商城页面点击**刷新**，新程序就会出现在「可安装」里。

---

## 二、sd.exe 与 Win+R：运行框里的极速启动

SuperDucker 的精神内核，就是 **`Win+R` 运行你注册过的程序**。

每个入口都有一个**「缩写」**（不区分大小写），例如百度的缩写是 `BDU`、7-Zip 是 `7ZI`。在 `Win+R` 运行框里键入缩写回车，软件就打开了。

### 以管理员身份运行

系统自带程序可以在运行框里用 `Shift+Ctrl+Enter` 提权，但外置程序往往不灵。这时就用上 `sd.exe` 了：

```bat
sd s 7zi      # 以管理员权限启动 7-Zip（弹出 UAC，点「是」即可）
sd d 7zi      # 直接打开 7-Zip 的安装根目录
```

更多命令请随时查阅：

```bat
sd help
```

---

## 三、为公司搭建内网应用商店

想让内部电脑的软件版本统一掌握在你手里？用 `superducker-repo.exe` 搭一个局域网商店。

1. 找一台常开的机器，把 `superducker-repo.exe` 放进去运行，商店服务随即启动（默认 `http://0.0.0.0:5180`）。
2. 把你打包好的程序丢进**那台服务器**的 `localshop/` 目录。
3. 在客户端 SuperDucker 的**设置页**填入选「商店服务地址」，例如 `http://192.168.110.8:5180`（别漏了默认端口）。
4. 点「测试链接」，显示已连接即成功。

重新打开客户端商城的「可安装」并刷新，局域网里的软件就出现了，点击即可安装/升级。

> 这样，团队装什么、升什么版本，都由你这一台仓库服务器说了算。

---

## 小结

SuperDucker 还有很多细节等你发掘，后续也会持续更新。

- 项目地址：https://github.com/weekey-ux/superducker
- 下载地址：见本帖附件（提供 GUI+CLI 与 Repo 服务两个独立压缩包，当前版本 v1.2.4）
